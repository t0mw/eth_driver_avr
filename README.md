# Ethernet driver for AVR
Super-simple ethernet driver and tcp/ip stack for avr - currently based only on enc28j60.

The enc28j60 allows to communicate over ethernet with maximum of 10 Mbps speed. It interfaces with a MCU over SPI and is used frequently in Arduino projects.